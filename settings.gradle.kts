// The artifact name on BOTH release channels - the ivy publication for
// GitHub releases takes it from here, and Maven Central from the
// coordinates below. One name, so a consumer can never end up with the
// same classes twice under two coordinates.
rootProject.name = "activeledger"
